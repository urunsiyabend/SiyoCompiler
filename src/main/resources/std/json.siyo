{
    import java "codeanalysis.SiyoRuntime"

    fn parse(s: string) -> map {
        return SiyoRuntime.jsonParse(s)
    }

    fn stringify(v: object) -> string {
        return SiyoRuntime.jsonStringify(v)
    }
}
